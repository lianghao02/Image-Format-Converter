# 實作計畫

## 目標與驗收條件

新增「長截圖分頁輔助」分頁，協助將手機長截圖依未來 Photo-Report-Generator／Word 圖框比例切成連續衍生圖。

- 保留原始圖片；匯出採原始像素裁切，不縮放、不重取樣、不在圖片烙印遮罩。
- 可設定圖框寬、高與重疊高度；預設為 8 cm × 17.5 cm、重疊 5 mm、PNG。
- 以圖框寬高比例計算每頁裁切高度；重疊區確實同時出現在相鄰兩頁。
- 手機框可預覽長圖、上下拖曳、滑鼠滾輪與鍵盤上下鍵檢視；可拖曳分割線微調切點。
- 預覽有重疊區遮罩、頁碼與原始像素範圍；匯出圖保持乾淨。
- 沿用影片分頁的左右兩等分外觀與按鈕分區原則。

## 不做範圍

- 不直接建立或編輯 Word 文件；交由 Photo-Report-Generator 處理。
- 不改變原始圖片 DPI 或嘗試提升文字細節。
- 不做 OCR、內容辨識或自動判斷文字斷行。
- 不以重疊遮罩或頁碼修改匯出圖片。

## 現況與限制

- 主程式為 .NET 8 WPF；現有圖片服務可作 PNG/JPG 編碼，影片分頁有手機框、鍵盤快捷鍵與右側工作區可參考。
- 目前沒有自動化影像測試素材；需準備一般長截圖、窄圖、極長圖及邊界文字圖做實測。
- Photo-Report-Generator 負責將衍生圖放入 Word 固定圖框；本功能僅依圖框比例分頁。

## 已確認決策

- 新功能以獨立第三分頁提供，不混入一般圖片轉檔。
- 匯出圖以來源像素直接裁切；分頁高度以 `來源寬度 × 圖框高 ÷ 圖框寬` 計算。
- 重疊像素以 `來源寬度 × 重疊實體高度 ÷ 圖框寬度` 計算。
- 手機預覽中的遮罩只協助檢視；輸出 PNG 不帶遮罩、分割線或頁碼。
- 大螢幕延續左側手機框與右側設定／頁面清單的工作流；小螢幕採可讀的垂直重排。

## 工作清單

- [x] 建立長截圖分頁模型、服務介面與像素分頁計算｜1170 × 10000px、8 × 17.5cm、5mm 案例驗證為 4 頁、73px 固定重疊。
- [ ] 新增第三分頁與左右工作區｜已完成骨架；待以實際視窗確認寬螢幕與窄螢幕均無控制項重疊。
- [ ] 實作手機框長圖預覽、縮放與上下檢視｜已完成拖曳、滾輪、↑／↓、Page Up／Page Down、Home／End；待以實際長截圖驗證。
- [ ] 實作分割線與重疊遮罩互動｜調整切點後，相鄰頁重疊範圍維持固定且預覽正確。
- [ ] 實作預覽清單、原始像素裁切與 PNG 匯出｜以長截圖實測輸出連續、無縮放、無遮罩殘留。
- [ ] 整理影片分頁按鈕分區｜輸入、播放定位、輸出位置、證物管理各自集中；既有快捷鍵仍可用。
- [ ] 更新使用說明與驗證紀錄｜QA、手動情境驗證及限制均有記錄。

## 風險與因應

- Word 圖框尺寸與 DPI 混淆：介面明確標示「僅用於分頁比例，不改變圖片像素」。
- 分割線切到重要文字：提供固定重疊遮罩與可拖曳切點，不做不可靠的自動文字判斷。
- 極長圖片的記憶體使用量：採按頁裁切與串流寫檔，避免同時複製所有頁面點陣圖。
- 鍵盤快捷鍵衝突：只有手機預覽取得焦點時攔截檢視按鍵；文字輸入欄維持原生行為。

## 驗證紀錄

- `scripts\qa.ps1`：建置成功，0 警告、0 錯誤。
- 固定案例：1170 × 10000px 來源、8 × 17.5cm 圖框、5mm 重疊，計算結果為 4 頁；起點為 0／2486／4972／7458px，相鄰頁重疊 73px，最後頁結束於 10000px。
- PNG 匯出案例：40 × 200px 凍結點陣來源、8 × 17.5cm 圖框、5mm 重疊，成功輸出 3 張 PNG；每張輸出寬度維持 40px，測試暫存資料已清除。
- 尚未使用實際長截圖進行預覽操作與 PNG 匯出驗證。

## 剩餘問題

- 長截圖支援的輸入格式範圍（建議初期 PNG、JPG、JPEG）。
- 是否需要輸出分頁對照紀錄檔（檔名、頁次、來源像素範圍）；暫不納入第一版核心範圍。
