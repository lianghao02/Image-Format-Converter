# 實作計畫

## 目標與驗收條件

- 更新檢查只在使用者按下按鈕並確認可能連線至 GitHub 後才發出請求。
- 影片截圖頁清楚區分「清空清單（保留檔案）」與「刪除本次截圖檔案」，後者必須顯示不可復原的確認訊息。
- 發布腳本在 `dist\` 產生 EXE、SHA-256 檔與簡短離線使用說明。
- 建立不依賴第三方測試套件的核心服務測試，並由 QA 腳本執行。

## 不做範圍

- 不增加背景更新、遙測、檔案上傳或自動下載更新。
- 不改動來源檔案或現有截圖輸出格式。
- 不將發行產物或測試建置快取提交至 Git。

## 已確認決策

- 更新確認對話框會清楚說明僅查詢 GitHub Release 版本資料，不會上傳本機檔案。
- 刪除功能僅處理本次 `Snapshots` 清單中的衍生截圖；刪除失敗的項目保留在清單中。
- 測試採用內建 Console 測試執行器，避免為單一專案引入額外 NuGet 測試相依性。

## 工作清單

- [x] 更新檢查確認流程與離線說明｜編譯通過，服務呼叫只位於使用者命令流程。
- [x] 截圖清單／檔案刪除語意｜介面文字與命令邏輯完成；刪除前顯示不可復原確認。
- [x] 發行雜湊與使用說明｜建置後檔案存在且 SHA-256 吻合。
- [x] 核心服務測試與 QA 整合｜4 項測試與 `qa.ps1` 通過。

## 風險與因應

- 刪除衍生截圖具有不可逆性：執行前二次確認，並保留無法刪除的清單項目。
- `MediaElement` 不保證解碼影格索引：本次只調整清單語意與更新安全，不改變影片解碼邊界。

## 驗證紀錄

- `scripts\qa.ps1`：C# Release 建置 0 警告、0 錯誤；4/4 項核心服務測試通過。
- `scripts\build.ps1`：成功產生 `dist\PoliceImageToolkit.exe`（68.52 MB）、`version.txt`、`SHA256SUMS.txt` 與 `使用說明.txt`。
- `PoliceImageToolkit.exe` SHA-256：`d4de14973aa4bd8347dd2923996532d04cd18d19b8ebe2d8fd5261e374cb72b4`，與 `SHA256SUMS.txt` 一致。

## 剩餘問題

- 無。
